namespace CV_1
{
    public struct Coordinates
    {
        public int x; // ---
        public int y; // |

        public Coordinates(int y, int x)
        {
            this.x = x;
            this.y = y;
        }
    }
}